<p style="color: green"><%= notice %></p>
<div class="page-header mb-4">
  <div class="page-header-content">
    <h1 class="page-title">Item Details</h1>
    <div class="page-actions">
      <%= link_to "← Back to Menu", items_path, {class: "btn btn-secondary"} %>
    </div>
  </div>
</div>
<div class="card">
  <div class="card-header">
    <h3 class="mb-0"><%= @item[0][:name].capitalize %></h3>
  </div>
  <div class="card-body">
    <%= render @item %>
  </div>
  <div class="d-flex card-footer gap-2">
    <%= link_to "✏️ Edit Item", edit_item_path(@item), {class: "btn card-button"} %>
    <%= button_to "🗑️ Delete Item", @item, method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure you want to delete this item?" } %>
    <%= link_to "➕ Create Order", new_order_path, {class: "btn btn-success"} %>
  </div>
</div>
