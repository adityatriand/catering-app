<p style="color: green"><%= notice %></p>
<div class="page-header mb-4">
  <div class="page-header-content">
    <h1 class="page-title">Order Details</h1>
    <div class="page-actions">
      <%= link_to "← Back to Orders", orders_path, {class: "btn btn-secondary"} %>
    </div>
  </div>
</div>
<div class="card">
  <div class="card-header">
    <h3 class="mb-0">📧 <%= @order[0][:email]%></h3>
  </div>
  <div class="card-body">
    <%= render @order %>
  </div>
  <div class="d-flex card-footer gap-2">
    <% if @order[0][:status_order] == 0 %>
      <%= link_to "✏️ Edit Order", edit_order_path(@order), {class: "btn card-button"} %>
    <% else %>
      <span class="alert alert-warning mb-0 d-flex align-items-center">
        This order cannot be edited (status: <%= ["NEW","PAID","CANCELED"][@order[0][:status_order]] %>)
      </span>
    <% end %>
    <%= button_to "🗑️ Delete Order", @order, method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure you want to delete this order?" } %>
  </div>
</div>
